(function ($) {
  $.fn.pluginName = function (options) {
    return this.each(function () {
      const defs = {.....}
      const opts = $.extend({}, defs, options)

      ......
    });
  };
})(jQuery);
